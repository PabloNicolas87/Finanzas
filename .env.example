# Configuración de entorno de desarrollo
# Copia este archivo a .env y ajusta los valores según tu configuración

# URL de conexión a PostgreSQL (Docker local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/finanzas"

# Puerto del backend (opcional, por defecto 3000)
PORT=3000
